#include <iostream>
#include <string>
#include <string.h>
using namespace std;

int main()
{
	string formula = " ";
	cin >> formula;

	for (int i = 0; i < formula.length(); i++)
	{
		if (formula[i] != '-' && formula[i] != '+')
		{

		}

	}

	return 0;
}